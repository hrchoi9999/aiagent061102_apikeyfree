# AI Agent 061102 API Key Free

Streamlit PDF question-answering practice app.

## Run locally

```powershell
streamlit run app.py
```

The app asks for an `OPENAI_API_KEY` in the page, so no API key is stored in the repository.
